# Versions are pinned (==) to the known-good set this project was built and
# tested against, for reproducible installs and a smaller supply-chain surface.
# Bump deliberately (and re-run `pytest`) rather than floating with `>=`.

# Core
langgraph==1.2.2
langchain==1.3.2
langchain-ollama==1.1.0
langchain-community==0.4.2
langchain-experimental==0.4.2  # For PythonREPLTool
langchain-core==1.4.0
langchain-text-splitters==1.1.2

# Memory / RAG
chromadb==1.5.9
langchain-chroma==1.1.0

# Audio transcription (local Whisper model — also needs ffmpeg on PATH)
faster-whisper==1.2.1

# Video understanding (process_video: yt-dlp fetch, ffmpeg scene-detect, Moondream
# keyframe descriptions via HF transformers). ffmpeg must be on PATH.
# NOTE: plain `pip install -r requirements.txt` gets the CPU torch wheel; for CUDA run
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
# first (the +cu128 build satisfies this pin, so -r won't downgrade it).
yt-dlp==2026.6.9
torch==2.11.0
transformers==4.53.3
einops==0.8.2
accelerate==1.14.0

# API
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
requests==2.34.2

# UI
streamlit==1.57.0

# Observability
langsmith==0.8.5

# Dev & testing
pytest==9.0.3
python-dotenv==1.2.2
